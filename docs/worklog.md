# 작업 기록

실제로 수행한 작업, 명령과 결과만 기록한다. 예상 명령과 가상의 결과는 작성하지 않는다.

## 2026-08-05 — 초기 문서 구성

### 환경

- 문서 생성 단계
- 실제 저장소 경로: 확인 필요
- 실제 브랜치: 확인 필요
- Python 버전: 미확인
- 운영체제 실행 검증: 미실행

### 변경 파일

- `AGENTS.md`
- `MISSION.md`
- `README.md`
- `.gitignore`
- `.gitattributes`
- `docs/requirements.md`
- `docs/progress.md`
- `docs/worklog.md`
- `docs/troubleshooting.md`
- `evidence/.gitkeep`

### 수행 내용

- 미션 원문을 필수 기능, 데이터, 성능, 문서와 제약으로 분류
- 요구사항 추적표 작성
- 단계별 구현 순서와 권장 커밋 계획 작성
- 실제 결과가 없는 항목을 모두 `예정`, `TODO`, `미검증`으로 표시
- `data.json` 상세 구조가 미확정임을 기록

### 실행 명령

없음.

### 실제 결과

- 코드 실행 없음
- 테스트 실행 없음
- 성능 측정 없음
- PASS/FAIL 결과 없음
- 증거 파일 없음

### 다음 작업

`mini_npu.py`에 MAC 연산과 epsilon 판정 로직을 구현하고 예시 데이터로 검증한다.

## 2026-08-06 — 단계 1 핵심 MAC과 판정 로직 구현

### 작업 시작 상태

- 브랜치: `main`
- 작업 트리: 변경사항 없음
- 최근 커밋: `e25d9d8 Chore: 프로젝트 초기 파일 구성`
- Python 버전: 3.12.13

### 변경 파일

- `AGENTS.md`
- `mini_npu.py`
- `README.md`
- `docs/requirements.md`
- `docs/progress.md`
- `docs/worklog.md`

### 수행 내용

- 사용자 승인 후 단계 1 범위만 구현
- 숫자로 구성된 정사각형 행렬 검증 구현
- 패턴과 필터 크기 일치 검증 구현
- 외부 라이브러리 없이 이중 반복문 MAC 계산 구현
- 기본 epsilon `1e-9` 기반 Cross/X/UNDECIDED 판정 구현
- NaN, 무한대, bool과 숫자가 아닌 값의 오류 처리 구현
- 구현과 동시에 요구사항, 진행 상태와 README 갱신
- Codex가 유닛 테스트를 작성·실행하지 않는 작업 방침을 `AGENTS.md`에 반영

### 실행 명령과 실제 결과

- 코드 실행 없음
- 유닛 테스트 작성 및 실행 없음
- 성능 측정 없음
- PASS/FAIL 결과 없음

### 다음 작업

사용자가 단계 1 핵심 로직을 직접 검증한다. 검증 결과가 확인되면 단계 2 사용자 입력 모드의 구현 방향을 설명하고 승인을 받는다.

## 2026-08-06 — 단계 2 사용자 입력 모드 구현

### 변경 파일

- `AGENTS.md`
- `main.py`
- `mini_npu.py` 삭제 후 `src/mini_npu.py`로 이동
- `src/__init__.py`
- `src/manual_mode.py`
- `README.md`
- `docs/requirements.md`
- `docs/progress.md`
- `docs/worklog.md`

### 수행 내용

- 향후 소스 증가를 고려해 핵심 모듈을 `src/` 패키지로 분리
- 루트 `main.py`에 실행 메뉴와 종료 처리 구현
- 3×3 필터 A(Cross), 필터 B(X), 패턴 입력 구현
- 열 개수, 숫자 형식, NaN과 무한대 오류 시 해당 행만 재입력하도록 구현
- 기존 MAC과 epsilon 판정을 사용자 입력 흐름에 연결
- Cross/X 점수와 사용자용 판정 결과 출력 구현
- `perf_counter_ns()`로 `calculate_mac()` 1회를 1,000회 반복한 평균 시간 계산 구현
- 측정 대상과 실제 반복 횟수를 콘솔 출력에 명시
- 필요한 로그와 캡처를 구현 후 안내하고 임시 Python 검증 예시는 생략하도록 작업 지침 갱신

### 실행 명령과 실제 결과

- 코드 실행 없음
- 유닛 테스트 작성 및 실행 없음
- 성능 측정 결과 없음
- PASS/FAIL 결과 없음

### 다음 작업

사용자가 `python3 main.py`로 사용자 입력 모드를 직접 검증한다. 결과가 확인되면 단계 3 JSON 로드와 라벨 정규화의 구현 방향을 설명하고 승인을 받는다.

## 2026-08-06 — 사용자 실행 결과 반영과 메뉴 분리

### 사용자 제공 실행 결과

- 실행 명령: `py`
- Cross 점수: `3.000000`
- X 점수: `6.000000`
- 판정: `X`
- 3×3 평균 MAC 시간: `0.018066 ms`
- 측정 대상: 패턴과 필터 A의 `calculate_mac()` 1회
- 반복 횟수: 1,000회

입력값을 위치별로 계산한 Cross 점수 3과 X 점수 6이 실제 출력과 일치했다. 오류 입력 재시도는 이 실행에서 확인하지 않았다.

### 변경 파일

- `main.py`
- `src/menu.py`
- `src/manual_mode.py`
- `AGENTS.md`
- `MISSION.md`
- `evidence/manual-mode-success.md`
- `README.md`
- `docs/requirements.md`
- `docs/progress.md`
- `docs/worklog.md`
- `docs/troubleshooting.md`

### 수행 내용

- `main.py`에서 메뉴 출력과 선택 처리를 제거해 실행 진입점만 유지
- 메뉴 흐름을 `src/menu.py`로 분리
- 실제 사용자 로그에 맞춰 종료 메뉴 번호를 3으로 통일
- `패턴를 입력하세요` 문구를 `패턴 행렬을 입력하세요`로 수정
- 실행 로그를 개인정보가 없는 Markdown 증거로 정리
- 정상 입력, 점수, X 판정과 3×3 성능 결과를 요구사항과 README에 반영

### Codex 실행 여부

- 코드 실행 없음
- 유닛 테스트 작성 및 실행 없음
- Git 명령 실행 없음

### 다음 작업

사용자가 오류 입력 후 같은 행을 다시 요청하는지 확인한다. 결과를 받은 뒤 단계 3 JSON 로드와 라벨 정규화의 구현 방향을 설명한다.

## 2026-08-06 — 사용자 입력 오류 재시도 결과 반영

### 사용자 제공 실행 결과

- 필터 A 2행에 숫자 4개 입력 후 열 개수 오류 메시지 확인
- 오류 뒤 필터 A 2행을 다시 요청하는 동작 확인
- 필터 A 3행에 문자 `q` 입력 후 숫자 형식 오류 메시지 확인
- 오류 뒤 필터 A 3행을 다시 요청하는 동작 확인
- 정상적으로 입력한 이전 행은 다시 요청하지 않음

### 변경 파일

- `evidence/invalid-input.md`
- `README.md`
- `docs/requirements.md`
- `docs/progress.md`
- `docs/worklog.md`

### 실제 결과

- `FUNC-007`: 필수 열 개수·숫자 형식 오류의 행 단위 재입력 확인
- `EVID-002`: Markdown 실행 로그 확보
- NaN과 무한대 입력 재시도는 실행하지 않음

### Codex 실행 여부

- 코드 실행 없음
- 유닛 테스트 작성 및 실행 없음
- Git 명령 실행 없음

### 다음 작업

단계 3 JSON 로드와 라벨 정규화의 구현 방향을 설명하고 사용자 승인을 받는다.

## 2026-08-06 — 단계 3 JSON 로드와 라벨 정규화 구현

### 변경 파일

- `src/data_loader.py`
- `src/json_mode.py`
- `src/menu.py`
- `AGENTS.md`
- `evidence/manual-mode-success.md`
- `evidence/invalid-input.md`
- `README.md`
- `MISSION.md`
- `docs/requirements.md`
- `docs/progress.md`
- `docs/worklog.md`

### 수행 내용

- 저장소 루트 기준 `data.json` UTF-8 로드 구현
- 파일 없음, 권한, 일반 읽기 오류와 JSON 문법 오류 구분 처리
- 최상위 `filters`, `patterns` 객체 검증 구현
- `size_N_idx` 패턴 키에서 크기 N 추출 구현
- 패턴 크기에 대응하는 `filters.size_N` 선택 구현
- expected `+`와 `x`를 Cross/X로 정규화
- 필터 키 `cross`와 `x`를 Cross/X로 정규화
- 패턴과 필터의 숫자 정사각형 구조 및 크기 일치 검증
- 케이스 한 건의 구조 오류가 다음 케이스 확인을 막지 않도록 처리
- 메뉴 2번에 JSON 로드 및 정규화 결과 출력 연결
- 사용자가 이미 변경한 간결한 evidence 파일명을 유지하고 문서 참조 갱신
- 향후 실행 로그 파일명에 요구사항 번호를 사용하지 않는 방침 반영

### Codex 실행 여부

- 코드 실행 없음
- 유닛 테스트 작성 및 실행 없음
- Git 명령 실행 없음
- JSON 로드 결과 미확인

### 다음 작업

사용자가 메뉴 2번을 실행해 필터 크기, 패턴 6개, 정규화 라벨과 로드 요약을 확인한다. 결과는 `evidence/json-load.md`에 기록한다.

## 2026-08-06 — JSON 로드와 정규화 실행 결과 반영

### 사용자 제공 실행 결과

- 필터 크기: 5, 13, 25
- 패턴 수: 6
- 5×5, 13×13, 25×25 패턴 각각 2건
- expected `x` 3건이 `X`로 출력됨
- expected `+` 3건이 `Cross`로 출력됨
- 모든 케이스에서 Cross/X 필터 연결 확인
- 정상 케이스: 6
- 구조 오류 케이스: 0

### 변경 파일

- `evidence/json-load.md`
- `README.md`
- `docs/requirements.md`
- `docs/progress.md`
- `docs/worklog.md`

### 실제 결과

- JSON 모드 진입과 정상 파일 로드 확인
- 패턴 키의 크기 추출 확인
- 패턴 크기에 맞는 필터 선택 확인
- expected와 필터 라벨의 내부 Cross/X 표준화 확인
- MAC 점수와 PASS/FAIL은 현재 출력하지 않음

### Codex 실행 여부

- 코드 실행 없음
- 유닛 테스트 작성 및 실행 없음
- Git 명령 실행 없음

### 다음 작업

단계 4 JSON 케이스별 MAC 판정, PASS/FAIL과 전체 결과 요약의 구현 방향을 설명하고 사용자 승인을 받는다.

## 2026-08-06 — 단계 4 JSON 일괄 판정과 결과 요약 구현

### 변경 파일

- `src/json_mode.py`
- `src/report.py`
- `src/menu.py`
- `README.md`
- `docs/requirements.md`
- `docs/progress.md`
- `docs/worklog.md`

### 수행 내용

- 검증된 패턴을 Cross/X 필터와 각각 MAC 계산하도록 연결
- epsilon 기준 Cross/X/UNDECIDED 판정 연결
- 실제 판정과 expected를 비교한 PASS/FAIL 구현
- 데이터 구조 오류와 MAC 계산 오류를 해당 케이스 FAIL로 변환
- 오류 케이스 뒤 다음 케이스 분석 계속 처리
- 케이스별 Cross/X 점수, 판정, expected, PASS/FAIL과 실패 사유 출력
- 전체 케이스, 통과, 실패 수와 판정 epsilon 출력
- 실패 케이스 식별자와 직접 원인 목록 출력
- 개별 결과와 전체 요약 출력을 `src/report.py`로 분리
- 메뉴 2번 이름을 `data.json 일괄 분석`으로 변경

### Codex 실행 여부

- 코드 실행 없음
- 유닛 테스트 작성 및 실행 없음
- Git 명령 실행 없음
- JSON 판정 결과 미확인

### 다음 작업

사용자가 메뉴 2번을 실행해 케이스별 점수와 PASS/FAIL, 전체 합계를 확인한다. 실제 결과는 `evidence/json-analysis.md`에 기록한다.

## 2026-08-06 — JSON 일괄 판정 실행 결과 반영

### 사용자 제공 실행 결과

- 전체 케이스: 6
- 통과: 3
- 실패: 3
- 판정 epsilon: `1e-09`
- 실패 케이스: `size_5_1`, `size_13_2`, `size_25_1`

### 실패 직접 원인

- `size_5_1`: Cross 0.9, X 0.9로 UNDECIDED, expected X
- `size_13_2`: Cross 7.5, X 7.5로 UNDECIDED, expected Cross
- `size_25_1`: Cross 4.9, X 4.9로 UNDECIDED, expected X

세 케이스 모두 epsilon 동점 정책에 따라 UNDECIDED가 되었고 expected와 불일치해 FAIL 처리됐다. 구조 또는 라벨 정규화 오류는 확인되지 않았다.

### 변경 파일

- `evidence/json-analysis.md`
- `README.md`
- `docs/requirements.md`
- `docs/progress.md`
- `docs/worklog.md`

### Codex 실행 여부

- 코드 실행 없음
- 유닛 테스트 작성 및 실행 없음
- Git 명령 실행 없음

### 다음 작업

단계 5 크기별 성능 분석의 구현 방향을 설명하고 사용자 승인을 받는다.

## 2026-08-06 — 단계 5 크기별 MAC 성능 분석 구현

### 변경 파일

- `src/performance.py`
- `src/json_mode.py`
- `src/report.py`
- `README.md`
- `docs/requirements.md`
- `docs/progress.md`
- `docs/worklog.md`

### 수행 내용

- 3×3·5×5·13×13·25×25 성능 측정 입력 구성
- 3×3은 코드 내부 Cross 기준 행렬 사용
- 5×5·13×13·25×25는 실제 JSON의 크기별 첫 정상 패턴과 Cross 필터 사용
- 각 크기에서 `calculate_mac()` 1회를 1,000회 반복 측정
- 평균 밀리초와 N² 연산 위치 수 9·25·169·625 계산
- 반복 횟수와 측정 입력 출처를 성능 표에 표시
- 행렬 검증과 MAC 포함, JSON 읽기와 콘솔 출력 제외 조건 명시
- 이중 반복문의 O(N²) 복잡도와 실제 측정 오차 안내 출력
- 성능 측정 책임을 `src/performance.py`로 분리

### Codex 실행 여부

- 코드 실행 없음
- 유닛 테스트 작성 및 실행 없음
- Git 명령 실행 없음
- 성능 측정값 미확인

### 다음 작업

사용자가 메뉴 2번을 실행해 JSON 결과 뒤의 크기별 성능 표를 확인한다. 실제 측정 결과는 `evidence/performance.md`에 기록한다.

## 2026-08-06 — 크기별 MAC 성능 실행 결과 반영

### 사용자 제공 측정 결과

| 크기 | 평균 시간(ms) | N² | 반복 횟수 |
|---|---:|---:|---:|
| 3×3 | 0.018819 | 9 | 1,000 |
| 5×5 | 0.040140 | 25 | 1,000 |
| 13×13 | 0.222042 | 169 | 1,000 |
| 25×25 | 0.773164 | 625 | 1,000 |

### 실제 결과 분석

- 네 필수 크기와 N² 연산 수가 모두 출력됨
- 모든 크기에서 1,000회 반복 확인
- 크기와 N²가 증가할수록 평균 시간 증가
- 13×13에서 25×25로 갈 때 N² 약 3.70배, 시간 약 3.48배 증가
- 작은 크기에서는 함수 호출·검증·Python 실행 고정 비용으로 N² 비율과 시간 비율 차이 발생
- 이중 반복문으로 모든 위치를 방문하므로 O(N²) 구조와 측정 경향이 일치

### 변경 파일

- `evidence/performance.md`
- `README.md`
- `docs/requirements.md`
- `docs/progress.md`
- `docs/worklog.md`

### Codex 실행 여부

- 코드 실행 없음
- 유닛 테스트 작성 및 실행 없음
- Git 명령 실행 없음

### 다음 작업

단계 6 최종 검수와 문서 정리의 범위를 설명하고 사용자 승인을 받는다.

## 2026-08-06 — 최신 작업 기준 문서 동기화

### 점검 결과

- 단계 1~5 구현 내용과 사용자 제공 실행 로그 5건의 연결 확인
- README의 모든 evidence 링크가 현재 파일과 연결됨을 확인
- 사용자 실행 점수, PASS/FAIL 합계, epsilon과 크기별 성능 수치가 문서 사이에서 일치함을 확인
- Markdown 파일에 계정명, 장치명과 개인 절대경로가 남아 있지 않음을 확인
- 크기 불일치, 파일 없음·손상 JSON과 최종 `python3 main.py` 재현은 미검증 상태로 유지

### 변경 파일

- `AGENTS.md`
- `MISSION.md`
- `README.md`
- `docs/requirements.md`
- `docs/progress.md`
- `docs/worklog.md`
- `docs/troubleshooting.md`
- `evidence/manual-mode-success.md`
- `evidence/json-load.md`
- `evidence/json-analysis.md`

### 수행 내용

- 현재 단계를 단계 6 최종 수동 검증과 문서 정리로 갱신
- 초기 예정 문구와 이미 완료된 검증 목록을 실제 상태로 교체
- MISSION의 과거 루트 모듈·자동 테스트 구조를 현재 `src/` 구조와 수동 검증 방침으로 교체
- 요구사항 추적 상태를 확보된 실행 증거 수준에 맞게 갱신
- 사용자가 직접 관리하는 Git 운영 계획과 Codex의 Git 확인 절차 제거
- 기존 실행 로그의 실제 출력은 수정하지 않고 후속 증거 문서 연결만 추가

### Codex 실행 여부

- 애플리케이션 실행 없음
- 유닛 테스트 작성 및 실행 없음
- Git 명령 실행 없음
- 문서와 소스의 정적 대조만 수행

### 다음 작업

사용자가 JSON 오류 처리 시나리오를 직접 실행한다. 실제 결과를 받은 뒤 해당 Markdown 증거와 요구사항 상태를 갱신한다.

## 2026-08-09 — JSON 파일 인코딩 오류 처리 보완

### 변경 파일

- `src/data_loader.py`
- `MISSION.md`
- `README.md`
- `docs/requirements.md`
- `docs/progress.md`
- `docs/worklog.md`

### 수행 내용

- 파일 없음·권한·JSON 문법 오류 처리와 별도로 UTF-8 디코딩 오류 처리 추가
- UTF-8이 아닌 `data.json`에서 발생하는 `UnicodeDecodeError`를 사용자용 `DataLoadError`로 변환
- JSON 모드가 예외로 종료되지 않고 `분석 실패: data.json 파일은 UTF-8 형식이어야 합니다.`를 출력한 뒤 모드를 종료하도록 연결
- 정상 JSON 로드, 케이스 단위 데이터 오류와 MAC 분석 흐름은 변경하지 않음
- README, 요구사항 추적표와 진행 상태에 구현 범위와 미검증 상태 반영

### Codex 실행 여부

- 애플리케이션 실행 없음
- 유닛 테스트 작성 및 실행 없음
- Git 명령 실행 없음
- 코드와 문서의 정적 대조만 수행

### 실행 검증 상태

- 코드 구현 완료
- 파일 없음·비 UTF-8·손상 JSON의 실제 사용자 실행 결과는 미확인

### 다음 작업

사용자가 세 파일 단위 오류 안내를 간단히 확인하고 관련 출력만 전달한다.

## 2026-08-09 — 간단한 구현 우선 작업 방침 반영

### 변경 파일

- `AGENTS.md`
- `docs/progress.md`
- `docs/worklog.md`

### 수행 내용

- 미션 필수 기능과 기본 Python 문법을 우선하는 원칙 추가
- 평가에 필요하지 않은 복잡한 문법, 과도한 예외 처리와 확장 기능을 피하도록 명시
- 역할이나 반복 제거 효과가 없는 함수·메서드·클래스 분리를 하지 않도록 명시
- 같은 요구사항을 충족하면 이해하기 쉬운 직접 구현을 선택하도록 작업 기준 정리

### Codex 실행 여부

- 애플리케이션 실행 없음
- 유닛 테스트 작성 및 실행 없음
- Git 명령 실행 없음

## 2026-08-09 — 최종 JSON 분석과 성능 재현 결과 반영

### 사용자 제공 실행 결과

- 실행 명령: `python3 main.py`
- JSON 전체 케이스: 6
- 통과: 3
- 실패: 3
- 판정 epsilon: `1e-09`
- 실패 케이스: `size_5_1`, `size_13_2`, `size_25_1`
- 네 크기 모두 `calculate_mac()` 1회 기준 1,000회 반복

### 최종 성능 결과

| 크기 | 평균 시간(ms) | N² | 반복 횟수 |
|---|---:|---:|---:|
| 3×3 | 0.019228 | 9 | 1,000 |
| 5×5 | 0.040938 | 25 | 1,000 |
| 13×13 | 0.230900 | 169 | 1,000 |
| 25×25 | 0.807215 | 625 | 1,000 |

### 확인 결과

- 이전 실행과 동일한 케이스별 점수·판정·PASS/FAIL 확인
- 통과 3 + 실패 3 = 전체 6 확인
- 세 실패 모두 epsilon 동점에 따른 의도된 `UNDECIDED` 확인
- N²와 행렬 크기가 증가할수록 평균 시간이 증가하는 경향 확인
- 13×13 대비 25×25에서 N² 약 3.70배, 시간 약 3.50배 증가

### 변경 파일

- `evidence/final-run.md`
- `MISSION.md`
- `README.md`
- `docs/requirements.md`
- `docs/progress.md`
- `docs/worklog.md`

### Codex 실행 여부

- 애플리케이션 실행 없음
- 유닛 테스트 작성 및 실행 없음
- Git 명령 실행 없음
- 사용자 제공 결과만 문서에 반영

### 다음 작업

필수 제출 상태를 최종 점검하고 관련 문서 사이의 수치와 상태를 정리한다.

## 2026-08-09 — 필수 미션 최종 점검과 완료 상태 반영

### 점검 결과

- 사용자 입력, JSON 일괄 판정, epsilon 동점 정책과 크기별 성능 요구사항 충족 확인
- MAC이 외부 라이브러리 없이 이중 반복문으로 구현됐음을 확인
- 3×3·5×5·13×13·25×25의 1,000회 성능 결과와 O(N²) 설명 확인
- 파일 오류는 모드 단위 종료, 케이스 오류는 FAIL 변환 후 다음 케이스 계속 처리하도록 구현됐음을 정적 확인
- `python3 main.py` 최종 정상 실행과 README 최신 수치 일치 확인
- 필수 제출을 막는 코드·문서 불일치 없음

### 사용자 결정

- JSON 파일 오류와 크기 불일치의 별도 실행 evidence는 생성하지 않음
- 실행하지 않은 오류 결과를 완료 증거로 작성하지 않고 코드 구현 확인으로 구분
- 필수 미션 완료 후 첫 보너스 방향을 검토

### 변경 파일

- `MISSION.md`
- `README.md`
- `docs/requirements.md`
- `docs/progress.md`
- `docs/worklog.md`
- `docs/troubleshooting.md`

### Codex 실행 여부

- 애플리케이션 실행 없음
- 유닛 테스트 작성 및 실행 없음
- Git 명령 실행 없음
- 소스, 문서와 사용자 제공 실행 결과의 정적 대조만 수행

### 다음 작업

보너스 1차원 MAC 최적화 비교의 구현 방향을 설명하고 사용자 승인을 받는다.

## 2026-08-09 — 보너스 1차원 MAC 최적화 비교 구현

### 변경 파일

- `src/mini_npu.py`
- `src/performance.py`
- `src/report.py`
- `src/json_mode.py`
- `MISSION.md`
- `README.md`
- `docs/requirements.md`
- `docs/progress.md`
- `docs/worklog.md`

### 수행 내용

- 정사각형 2차원 행렬을 행 순서의 1차원 리스트로 변환하는 `flatten_matrix()` 구현
- 같은 길이의 1차원 패턴과 필터를 단일 반복문으로 계산하는 `calculate_mac_1d()` 구현
- 1차원 입력의 길이와 유한 숫자 값을 기본 반복문으로 검증
- 기존 2차원 MAC과 1차원 MAC의 점수 일치 여부 확인
- 기존 2차원 성능 결과를 재사용하고 같은 반복 횟수로 1차원 MAC 측정
- 2차원·1차원 평균 시간과 `2D/1D` 속도비를 네 크기 비교 표로 출력
- 2차원에서 1차원으로 변환하는 시간과 콘솔 출력은 측정에서 제외
- 메뉴 2번의 기존 크기별 성능 표 다음에 보너스 표 연결

### 구현 단순화 기준

- 외부 라이브러리와 새로운 메뉴를 추가하지 않음
- 기존 모듈 안에 변환, 1차원 MAC과 비교 측정만 추가
- 필수 기능의 기존 출력과 계산 흐름은 변경하지 않음

### Codex 실행 여부

- 애플리케이션 실행 없음
- 유닛 테스트 작성 및 실행 없음
- Git 명령 실행 없음
- 코드와 문서의 정적 대조만 수행

### 실행 검증 상태

- 코드 구현 완료
- 실제 점수 일치 여부와 2차원·1차원 성능값은 사용자 실행 검증 전
- 실행하지 않은 측정값과 속도비는 문서에 작성하지 않음

### 다음 작업

사용자가 메뉴 2번을 실행하고 보너스 비교 표를 전달한다.

## 2026-08-09 — 보너스 성능 측정 흐름 단순화

### 변경 파일

- `src/performance.py`
- `src/report.py`
- `src/json_mode.py`
- `MISSION.md`
- `README.md`
- `docs/requirements.md`
- `docs/progress.md`
- `docs/worklog.md`

### 변경 이유

실행 중에 기존 2차원 MAC을 추가 계산하고 속도비를 즉시 만드는 것보다, 1차원 방식의 성능만 같은 조건으로 측정한 뒤 기존 2차원 실행 기록과 문서에서 분석하는 흐름이 보너스 목적에 더 직접적이다.

### 수행 내용

- 보너스 측정 중 추가 2차원 MAC 점수 계산 제거
- 점수 일치 여부와 실행 중 속도비 계산 제거
- 기존 `PerformanceResult`를 그대로 사용해 별도 비교 결과 클래스 제거
- 1차원 평균 시간, N² 연산 수, 반복 횟수와 입력 출처만 표로 출력
- 2차원 성능 표와 1차원 성능 표의 실제 기록을 받은 뒤 문서에서 비교하도록 변경
- 1차원 변환 시간과 콘솔 출력 제외 조건 유지

### Codex 실행 여부

- 애플리케이션 실행 없음
- 유닛 테스트 작성 및 실행 없음
- Git 명령 실행 없음
- 코드와 문서의 정적 대조만 수행

### 실행 검증 상태

- 단순화 구현 완료
- 1차원 실제 측정값은 사용자 실행 검증 전

### 다음 작업

사용자가 메뉴 2번의 `[보너스: 1차원 MAC 성능]` 출력 구간을 전달한다.

## 2026-08-09 — 보너스 1차원 MAC 성능 결과 반영

### 사용자 제공 실행 결과

| 크기 | 2차원 평균(ms) | 1차원 평균(ms) | 반복 횟수 |
|---|---:|---:|---:|
| 3×3 | 0.018674 | 0.012011 | 1,000 |
| 5×5 | 0.039624 | 0.030537 | 1,000 |
| 13×13 | 0.223035 | 0.191584 | 1,000 |
| 25×25 | 0.765619 | 0.729598 | 1,000 |

### 비교 결과

- 네 크기 모두 1차원 MAC의 평균 시간이 더 짧게 측정됨
- 3×3은 약 1.55배 빠르고 시간이 약 35.7% 감소
- 5×5는 약 1.30배 빠르고 시간이 약 22.9% 감소
- 13×13은 약 1.16배 빠르고 시간이 약 14.1% 감소
- 25×25는 약 1.05배 빠르고 시간이 약 4.7% 감소
- 두 방식 모두 N²개 위치를 처리하므로 시간 복잡도는 O(N²)
- 변환 시간은 1차원 측정 구간에서 제외됨

### 변경 파일

- `evidence/one-dimensional-performance.md`
- `MISSION.md`
- `README.md`
- `docs/requirements.md`
- `docs/progress.md`
- `docs/worklog.md`

### Codex 실행 여부

- 애플리케이션 실행 없음
- 유닛 테스트 작성 및 실행 없음
- Git 명령 실행 없음
- 사용자 제공 측정값만 기록하고 분석

### 다음 작업

보너스 2 N×N Cross/X 패턴 생성기의 구현 방향을 설명하고 사용자 승인을 받는다.

## 2026-08-09 — 미션 원문 보관과 현재 데이터 흐름 확인

### 변경 파일

- `docs/mission-original.md`
- `MISSION.md`
- `README.md`
- `docs/worklog.md`

### 수행 내용

- 사용자가 첨부한 E1-3 MiniNPU 미션 원문을 `docs/mission-original.md`에 보관
- `MISSION.md`가 구현용 요약 문서이고 원문은 별도 문서라는 관계를 명시
- README 프로젝트 구조와 문서 목록에 미션 원문 링크 추가
- 현재 모드 1의 콘솔 입력 행렬과 모드 2의 JSON 행렬이 Python의 2차원 리스트로 메모리에 유지되는 흐름을 코드 기준으로 확인
- 현재 두 모드는 실행 상태와 입력 데이터를 공유하지 않으며 JSON 쓰기 기능도 없음을 확인
- 생성 패턴의 구체적인 재사용 방식은 구현하지 않고 다음 설계 단계로 남김

### Codex 실행 여부

- 애플리케이션 실행 없음
- 유닛 테스트 작성 및 실행 없음
- Git 명령 실행 없음
- 원문 파일과 현재 코드의 정적 확인만 수행

### 다음 작업

미션 원문의 재사용 요구사항을 기준으로 생성 패턴을 모드 1과 성능 분석에 연결하는 간단한 구조를 정하고 사용자 승인을 받는다.

## 2026-08-09 — N×N 필터 생성과 모드 간 성능 재사용 구현

### 변경 파일

- `src/pattern_generator.py`
- `src/menu.py`
- `src/manual_mode.py`
- `src/json_mode.py`
- `src/performance.py`
- `MISSION.md`
- `README.md`
- `docs/requirements.md`
- `docs/progress.md`
- `docs/worklog.md`

### 수행 내용

- 메뉴 3번에 N×N Cross/X 필터 생성 기능 추가
- 3 이상의 정수 N을 입력받아 0.0과 1.0의 2차원 리스트 생성
- 홀수 Cross는 가운데 한 행·열, 짝수 Cross는 가운데 두 행·열로 구성
- X는 왼쪽 위·오른쪽 아래와 오른쪽 위·왼쪽 아래 대각선으로 구성
- 생성 필터를 JSON에 저장하지 않고 크기별 실행 중 딕셔너리에 보관
- 같은 크기를 다시 생성하면 해당 크기의 필터를 교체
- 모드 1에 기존 3×3 직접 입력과 N×N 저장·자동 생성 필터 사용 선택 추가
- 모드 1에서 사용한 필터와 패턴을 실행 중 저장소에 보관
- 각 모드 완료 후 메뉴로 돌아가 같은 프로그램 실행 안에서 데이터를 재사용하도록 변경
- 모드 2 성능 측정에서 필수 3·5·13·25 크기와 실행 중 추가한 크기를 함께 처리
- 2차원과 1차원 성능 측정이 동일한 저장 패턴과 Cross 필터를 사용하도록 연결

### Codex 실행 여부

- 애플리케이션 실행 없음
- 유닛 테스트 작성 및 실행 없음
- Git 명령 실행 없음
- 함수 호출 관계와 문서의 정적 확인만 수행

### 실행 검증 상태

- 코드 구현 완료
- 메뉴 표시, 생성 행렬, 저장 필터 재사용과 추가 크기 성능 출력은 사용자 실행 검증 전
- 실행하지 않은 점수와 측정값은 문서에 작성하지 않음

### 다음 작업

사용자가 메뉴 3 → 메뉴 1의 N×N 방식 → 메뉴 2 순서로 실행하고 생성 필터 재사용과 추가 크기 성능 표를 확인한다.

## 2026-08-09 — 생성 필터와 모드 2 성능 재사용 결과 반영

### 사용자 제공 실행 결과

- 메뉴 3에서 3×3 Cross/X 필터 생성 모양 확인
- 생성 3×3 필터의 실행 중 저장 안내 확인
- 생성 직후 모드 2의 JSON 결과가 전체 6건·통과 3건·실패 3건으로 기존과 동일함 확인
- 3×3 2차원 평균 `0.016889 ms`, 입력 출처 `자동 생성 3x3 Cross 패턴` 확인
- 3×3 1차원 평균 `0.012113 ms`, 입력 출처 `자동 생성 3x3 Cross 패턴` 확인
- 메뉴 복귀 후 7×7 Cross/X 필터 생성 모양과 저장 안내 확인

### 변경 파일

- `evidence/generated-filter-performance.md`
- `README.md`
- `docs/requirements.md`
- `docs/progress.md`
- `docs/worklog.md`

### 확인 결과

- 생성기가 3×3과 7×7에서 의도한 Cross/X 위치를 출력함
- 생성한 3×3 행렬이 같은 실행의 2차원·1차원 성능 분석에 재사용됨
- 생성 저장소와 JSON 케이스별 판정 흐름이 분리되어 기존 PASS/FAIL 결과가 유지됨

### 미검증 항목

- 모드 1의 기존 3×3 직접 입력 하위 메뉴
- 모드 1의 N×N 저장 필터 사용과 크기 미존재 시 자동 생성
- 생성 7×7 필터의 모드 2 성능 표 포함
- 같은 크기 재생성 시 실제 값 교체

### Codex 실행 여부

- 애플리케이션 실행 없음
- 유닛 테스트 작성 및 실행 없음
- Git 명령 실행 없음
- 사용자 제공 로그만 정리해 기록

### 다음 작업

긴 직접 입력 검증은 생략 가능하다. 보너스 전체 연결을 확인하려면 작은 크기로 모드 1 하위 2번의 저장 필터 사용과 자동 생성 중 하나만 추가 확인한다.

## 2026-08-09 — 생성기와 성능 출력 가독성 정리

### 변경 파일

- `src/report.py`
- `src/performance.py`
- `src/pattern_generator.py`
- `MISSION.md`
- `README.md`
- `docs/requirements.md`
- `docs/progress.md`
- `docs/worklog.md`

### 수행 내용

- 미션 원문의 성능 표 필수 항목이 크기·평균 시간·N²이고 입력 출처는 필수가 아님을 확인
- 2차원·1차원 콘솔 성능 표에서 입력 출처 열 제거
- 더 이상 사용하지 않는 성능 결과와 실행 중 저장소의 입력 출처 필드 제거
- 성능 표 아래의 측정 조건 안내 문구 제거
- 터미널 정렬을 위해 두 성능 표를 같은 영문 고정폭 열로 통일
- 필수 크기와 실행 중 생성 크기를 합쳐 숫자 오름차순으로 출력
- 필터 생성 크기를 3 이상의 홀수로 제한
- 생성된 필터를 하나의 제목 아래 Cross와 X 구역으로 구분
- 크기가 처음 추가됐으면 생성 완료, 기존에 있었으면 교체 완료 메시지만 출력
- README에는 파일 읽기와 콘솔 출력을 제외한다는 실제 측정 범위를 유지

### Codex 실행 여부

- 애플리케이션 실행 없음
- 유닛 테스트 작성 및 실행 없음
- Git 명령 실행 없음
- 코드와 문서의 정적 확인만 수행

### 실행 검증 상태

- 출력 정리 코드 구현 완료
- 홀수 입력 안내, 생성·교체 메시지와 오름차순 표는 사용자 실행 검증 전

### 다음 작업

사용자가 메뉴 3에서 홀수 필터를 생성한 뒤 메뉴 2를 실행해 간소화된 표 정렬과 크기 오름차순을 확인한다.

## 2026-08-09 — 간소화된 생성기와 성능 출력 실행 결과 반영

### 사용자 제공 실행 결과

- 메뉴 3에서 5×5 Cross/X 필터 생성 확인
- 첫 생성에서 `완료: 5x5 필터를 생성했습니다.` 확인
- 같은 실행에서 재생성 후 `완료: 기존 5x5 필터를 교체했습니다.` 확인
- JSON 분석 전체 6건·통과 3건·실패 3건 유지 확인
- 입력 출처 열과 측정 조건이 제거된 고정폭 성능 표 확인
- 3·5·13·25 크기가 오름차순으로 출력됨을 확인
- 2차원·1차원 각 크기의 1,000회 반복과 N² 값 확인

### 변경 파일

- `evidence/clean-performance-output.md`
- `MISSION.md`
- `README.md`
- `docs/requirements.md`
- `docs/progress.md`
- `docs/worklog.md`

### 최신 성능 결과

| 크기 | 2차원 평균(ms) | 1차원 평균(ms) | 반복 횟수 |
|---|---:|---:|---:|
| 3×3 | 0.018682 | 0.012007 | 1,000 |
| 5×5 | 0.039585 | 0.030298 | 1,000 |
| 13×13 | 0.217348 | 0.194398 | 1,000 |
| 25×25 | 0.758590 | 0.733237 | 1,000 |

### 확인 결과

- 신규 생성과 기존 교체 메시지가 실제 저장 상태에 맞게 출력됨
- Cross와 X 필터 구역이 구분되어 출력됨
- 간소화된 두 성능 표의 열 구성이 일치함
- 네 크기 모두 이번 실행에서 1차원 평균 시간이 더 짧게 측정됨

### 미검증 항목

- 짝수 또는 3 미만 입력의 재입력 안내
- 생성 7×7처럼 필수 크기 사이에 추가되는 행의 오름차순 위치
- 모드 1의 N×N 저장 필터 사용과 자동 생성

### Codex 실행 여부

- 애플리케이션 실행 없음
- 유닛 테스트 작성 및 실행 없음
- Git 명령 실행 없음
- 사용자 제공 로그만 정리해 기록

### 다음 작업

모드 1 N×N 흐름을 확인하거나, 해당 검증을 생략한다면 현재 보너스 결과를 최종 문서 상태로 정리한다.

## 2026-08-10 — 모드 1 N×N 자동 생성·저장 필터 사용 확인

### 사용자 제공 실행 정보

- 실행 명령: `py`
- 메뉴 1의 하위 2번에서 크기 3 선택
- `3x3 저장 필터가 없어 자동으로 생성합니다.` 안내 확인
- 입력 패턴: `0 1 0` / `1 1 0` / `0 1 0`
- Cross 점수: `4.000000`
- X 점수: `1.000000`
- 판정: `Cross`
- 단일 Cross MAC 1,000회 평균: `0.018306 ms`
- 실행 후 메인 메뉴 복귀 확인
- 같은 프로그램 실행에서 크기 3을 다시 선택했을 때 `3x3 저장 필터를 사용합니다.` 안내 확인

### 확인 결과

- `data.json`에 없는 크기의 필터 자동 생성 흐름 확인
- 자동 생성 Cross/X 필터를 사용한 패턴 판정 확인
- 모드 1의 단일 Cross MAC 성능 출력 확인
- 자동 생성 필터가 같은 프로그램 실행의 저장소에 유지되고 다시 선택되는 흐름 확인
- 두 번째 실행의 패턴, 점수와 성능값은 전달받지 않아 기록하지 않음
- 터미널 프롬프트의 계정명과 장치명은 증거에서 제거함

### 변경 파일

- `evidence/manual-nxn-filter-flow.md`
- `MISSION.md`
- `README.md`
- `docs/requirements.md`
- `docs/progress.md`
- `docs/worklog.md`

### 별도 실행하지 않은 추가 항목

- 짝수 또는 3 미만 크기 입력의 재입력 안내
- 생성한 7×7의 모드 2 성능 표 삽입 위치

두 항목은 미션 원문의 보너스 필수 산출물이 아니므로 현재 검증 범위에서 제외했다.

### Codex 실행 여부

- 애플리케이션 실행 없음
- 유닛 테스트 작성 및 실행 없음
- 사용자가 전달한 실제 출력과 확인 결과만 문서에 반영

### 다음 작업

필수 미션과 미션 원문의 보너스 범위에서 추가 구현 작업은 없다. 사용자가 최신 문서 변경사항을 확인한 뒤 직접 커밋한다.

## 2026-08-11 — 성능 반복 횟수와 1차원 출력 제목 조정

### 변경 파일

- `src/manual_mode.py`
- `src/performance.py`
- `src/report.py`
- `src/json_mode.py`
- `MISSION.md`
- `README.md`
- `docs/requirements.md`
- `docs/progress.md`
- `docs/worklog.md`

### 수행 내용

- 사용자 입력 모드의 MAC 평균 측정을 1,000회에서 100회로 변경
- 크기별 2차원·1차원 성능 측정을 모두 1,000회에서 100회로 변경
- 콘솔 제목 `[보너스: 1차원 MAC 성능]`을 `[1차원 MAC 성능]`으로 변경
- 관련 오류 문구에서 `보너스` 표현 제거
- 기존 1,000회 실행 로그와 측정값은 당시 기록으로 보존
- 현재 100회 기준 결과는 사용자 실행 전 상태로 문서에 표시

### Codex 실행 여부

- 애플리케이션 실행 없음
- 유닛 테스트 작성 및 실행 없음
- 실제 평균 시간과 PASS/FAIL 결과 생성 없음

### 다음 작업

사용자가 `python3 main.py`의 메뉴 2를 실행해 두 성능 표의 반복 횟수 100과 `[1차원 MAC 성능]` 제목을 확인한다.

## 2026-08-11 — AI 사전평가 성능·문서 보완

### 변경 파일

- `src/mini_npu.py`
- `src/manual_mode.py`
- `src/performance.py`
- `src/report.py`
- `src/json_mode.py`
- `src/data_loader.py`
- `MISSION.md`
- `README.md`
- `docs/requirements.md`
- `docs/progress.md`
- `docs/worklog.md`

### 수행 내용

- 사용자 지시에 따라 성능 반복 횟수를 100회에서 정확히 10회로 변경
- 하나의 전체 구간을 나누는 방식 대신 MAC 호출 10개를 각각 `perf_counter_ns()`로 측정
- 10개 측정값의 산술평균과 모집단 표준편차를 2차원·1차원 표에 출력
- 사용자 입력 모드에도 10회 평균과 표준편차 출력 적용
- UNDECIDED 발생 시 패턴과 필터를 확인하도록 안내 문구 추가
- JSON 실패를 `DATA`, `COMPUTE`, `TIE`, `MISMATCH` 유형으로 분류해 개별 결과와 요약에 출력
- 미션 원문의 실행 순서에 맞춰 JSON 결과 요약을 성능 표 뒤에 출력
- 잘못된 패턴 키 오류에 올바른 예시 `size_5_1` 추가
- 라벨 정규화 함수의 분리 이유를 docstring에 기록
- README 결과 리포트 내부에 실패 원인, MAC 계산 예시, epsilon 선택 기준과 모드별 동점 정책 설명 추가
- README에 파일 오류 안내 형식, 허용 라벨과 라벨 확장 체크리스트 추가
- README에 1000×1000급 행렬의 시간·메모리 문제, 직렬 CPU 병목, Python 병렬화 한계, 블록·희소 처리와 CPU/NPU 전환점 분석 추가
- 기존 1,000회 실행 로그와 실제 값은 과거 증거로 보존
- 실행하지 않은 NaN·inf 로그, 10회 성능값과 CPU 모델은 작성하지 않고 재검증 항목으로 유지

### 적용하지 않은 평가 제안

- 명세에 없는 expected 라벨 `plus`는 추가하지 않음
- 미션의 반복문 직접 구현과 외부 라이브러리 금지 범위를 벗어나는 병렬·NPU 코드는 추가하지 않고 README 분석으로만 정리
- 사용자 실행이 없는 NaN·inf, 파일 오류와 새 성능 결과를 실제 로그처럼 생성하지 않음

### Codex 실행 여부

- 애플리케이션 실행 없음
- 유닛 테스트 작성 및 실행 없음
- 실제 평균·표준편차와 PASS/FAIL 결과 생성 없음

### 다음 작업

사용자가 메뉴 2를 실행해 10회 평균·표준편차, 실패 유형과 동점 안내를 확인하고 실제 출력과 CPU 모델을 전달한다.

## 2026-08-11 — 사전평가 보완 범위 재정리와 README 간소화

### 변경 파일

- `src/data_loader.py`
- `src/json_mode.py`
- `src/manual_mode.py`
- `src/report.py`
- `README.md`
- `docs/progress.md`
- `docs/worklog.md`

### 수행 내용

- 사용자 후속 지시에 따라 10회 독립 측정의 평균과 표준편차 출력은 유지
- 미션 필수 범위가 아닌 실패 유형 시스템과 UNDECIDED 후속 행동 안내 제거
- 잘못된 패턴 키 수정 예시와 정규화 함수 설명용 추가 docstring 제거
- README의 긴 구현 목록, 오류 예시, 라벨 확장 체크리스트와 중복 실행 예시 제거
- README를 실행 환경, 핵심 구현, 성능 기준, 결과 리포트와 검증 상태 중심으로 재구성
- 실패 원인, epsilon 정책, O(N²), 대규모 행렬, 1차원 접근과 CPU/NPU 차이를 간결하게 정리
- 실제 확인된 Python 3.12.13만 기록하고 macOS 세부 버전과 CPU 모델은 미검증 유지
- 기존 1,000회 측정값은 과거 실행 기록으로 보존하고 현재 10회 결과와 구분

### Codex 실행 여부

- 애플리케이션 실행 없음
- 유닛 테스트 작성 및 실행 없음
- 새 측정값과 실행 로그 생성 없음

### 다음 작업

사용자가 메뉴 2를 실행해 10회 평균·표준편차 출력을 확인하고 실제 결과를 전달한다.

## 2026-08-11 — 대규모 최적화 분석 보강과 성능 표 한글화

### 변경 파일

- `src/report.py`
- `README.md`
- `docs/requirements.md`
- `docs/progress.md`
- `docs/worklog.md`

### 수행 내용

- 2차원·1차원 성능 표의 열 이름을 한글로 변경하고 ms와 N² 단위 유지
- README에 1000×1000 행렬의 위치 연산 수, Python 리스트 메모리와 반복 검증 비용 설명 추가
- 복사·변환 최소화, 1차원 접근, 검증 분리, 희소 필터, 블록 처리와 병렬 처리 대안 설명 추가
- 위 최적화는 미션 코드에 구현하지 않고 확장 방향으로만 명시
- CPU의 범용 코어·SIMD·스레드 처리와 현재 Python 반복문의 한계 설명 추가
- NPU의 병렬 MAC·내부 데이터 재사용 특성과 전송·대역폭·장치 활용률 병목 설명 추가
- 작은 행렬과 큰 반복 배치에서 CPU/NPU의 상대적 장점이 달라지는 이유 설명 추가

### Codex 실행 여부

- 애플리케이션 실행 없음
- 유닛 테스트 작성 및 실행 없음
- 새 측정값과 실행 로그 생성 없음

### 다음 작업

사용자가 메뉴 2를 실행해 한글 성능 표와 10회 평균·표준편차를 확인하고, 성능 환경 기록을 위해 macOS·CPU·Python 정보를 전달한다.

## 2026-08-11 — 10회 성능 측정과 macOS 환경 증거 반영

### 사용자 제공 실행 정보

- 실행 명령: `py`
- 실행 모드: 메뉴 2 `data.json` 일괄 분석
- 2차원·1차원 성능 표에서 3×3·5×5·13×13·25×25를 각각 10회 측정
- 평균 시간, 모집단 표준편차, N² 연산 수와 반복 횟수의 한글 열 출력 확인
- JSON 결과: 전체 6건, 통과 3건, 실패 3건
- 실패 3건 모두 동점 `UNDECIDED`와 expected 불일치임을 확인
- 두 성능 표가 케이스별 결과와 전체 결과 요약 사이에 출력됨을 확인

### 실제 성능 결과

| 크기 | 2차원 평균(ms) | 2차원 표준편차(ms) | 1차원 평균(ms) | 1차원 표준편차(ms) |
|---|---:|---:|---:|---:|
| 3×3 | 0.019748 | 0.000796 | 0.013336 | 0.002152 |
| 5×5 | 0.040439 | 0.000604 | 0.030974 | 0.000269 |
| 13×13 | 0.225531 | 0.000231 | 0.197598 | 0.000886 |
| 25×25 | 0.792834 | 0.012556 | 0.742221 | 0.014094 |

### 실행 환경

- Python: 3.12.13
- 운영체제: macOS Sequoia 15.7.4
- CPU: 3.1GHz 6코어 Intel Core i5
- 메모리: 32GB 2667MHz DDR4
- 사용자가 `evidence/mac-status.png`에 환경 화면을 저장하고 일련번호를 가림 처리함

### 변경 파일

- `evidence/ten-run-performance.md`
- `evidence/mac-status.png` (사용자 제공)
- `MISSION.md`
- `README.md`
- `docs/requirements.md`
- `docs/progress.md`
- `docs/worklog.md`

### 확인 결과

- 현재 10회 성능 측정 구현과 실제 출력이 일치함
- 네 필수 크기의 2차원·1차원 평균과 표준편차 증거 확보 완료
- 이번 실행에서는 네 크기 모두 1차원 평균 시간이 더 짧았음
- README의 성능값과 환경 정보를 실제 사용자 결과로 갱신함
- 메뉴 1의 현재 10회 평균·표준편차 출력은 이번 실행 범위에 포함되지 않음

### Codex 실행 여부

- 애플리케이션 실행 없음
- 유닛 테스트 작성 및 실행 없음
- 사용자가 제공한 실제 출력과 환경 캡처만 문서에 반영

### 다음 작업

변경사항을 최종 확인한 뒤 사용자가 직접 커밋한다.

## 2026-08-12 — README 실행 증거와 결과 리포트 정리

### 변경 파일

- `README.md`
- `docs/progress.md`
- `docs/worklog.md`

### 수행 내용

- 미션 원문의 README 권장 순서인 실행 방법, 구현 요약과 결과 리포트 구성을 유지
- 사용자 입력 모드의 Cross 3, X 6, 판정 X 결과를 소수 첫째 자리로 요약하고 콘솔 원문은 기존 실행 로그로 연결
- JSON 모드의 최신 전체 6건·통과 3건·실패 3건 집계를 10회 실행 로그에서 발췌
- 사용자 입력, 잘못된 입력, JSON 판정, 성능 측정과 필터 생성·재사용 설명 가까이에 해당 증거 링크 추가
- 과거 메뉴 1 로그의 1,000회 성능값은 현재 10회 결과 예시에 포함하지 않고 과거 기록임을 명시
- 대규모 행렬 분석에서 희소 필터, 블록 처리와 Python 프로세스 등 미션에서 직접 다루지 않은 심화 설명 제거
- CPU 병렬 처리와 NPU의 대량 MAC 특화 설명은 유지하고 미검증 하드웨어 세부 분석을 축약
- 성능값, 10회 평균·표준편차와 O(N²) 설명은 유지하고 시간 비율 차이의 원인을 Python 실행 오버헤드와 운영체제 스케줄링 중심으로 단순화
- 선택 기능 명칭을 `필터 생성기`로 유지하고 실행 중 사용자 모드·성능 분석 재사용을 명시
- 실제 검증 환경을 캡처 링크 없이 독립된 표로 유지하고 프로젝트 구조를 README 앞쪽으로 이동
- README에서 실행 날짜와 실행 주체 같은 이력성 설명을 제거
- FAIL 케이스 원인 분석과 성능표 해석을 별도 소제목으로 구분
- 대규모 행렬 대안을 방법별 목록으로 정리하고 CPU/NPU 설명을 보너스 뒤의 독립 구획으로 분리
- 현재 시간 측정이 `calculate_mac()` 내부 검증을 포함한다는 점을 코드로 확인했으며 측정 로직은 수정하지 않음
- 새로운 기능, 실행 결과와 측정값은 추가하지 않음

### Codex 실행 여부

- 애플리케이션 실행 없음
- 유닛 테스트 작성 및 실행 없음
- 기존 사용자 실행 로그와 README 문구만 정적으로 대조

### 다음 작업

사용자가 README의 구성, 실제 출력 발췌와 분석 축약 수준을 검토한다. 추가 조정이 없으면 변경 파일을 최종 확인한 뒤 사용자가 직접 커밋한다.

## 2026-08-12 — 입력 검증을 제외한 순수 MAC 성능 측정 구현

### 변경 파일

- `src/mini_npu.py`
- `src/performance.py`
- `MISSION.md`
- `README.md`
- `docs/requirements.md`
- `docs/progress.md`
- `docs/worklog.md`

### 수행 내용

- `calculate_mac()`에서 정사각형·숫자·크기 검증을 제거하고 검증된 같은 크기 행렬의 곱셈·누적만 수행하도록 변경
- `calculate_mac_1d()`에서 벡터 검증을 제거하고 검증된 같은 길이 벡터의 곱셈·누적만 수행하도록 변경
- 입력이 타이머 전에 숫자로 검증되므로 2차원·1차원 MAC 반복문 내부의 매 원소 `float()` 변환도 제거
- 일반 사용자 입력은 `read_matrix()`의 행 크기·숫자·유한값 검증을 그대로 사용
- JSON 판정은 `build_pattern_case()`와 `get_filter_pair()`의 행렬·크기 검증을 그대로 사용
- 2차원 성능 측정은 반복 전에 패턴과 필터를 각각 검증하고 크기 일치를 확인
- 2차원 타이머 안에는 검증 없는 `calculate_mac()` 호출만 포함
- 1차원 성능 측정은 타이머 전에 `flatten_matrix()`로 두 행렬을 검증·변환하고 두 벡터의 길이 일치를 확인
- 1차원 타이머 안에는 검증 없는 `calculate_mac_1d()` 호출만 포함
- 기존 검증 포함 성능값을 현재 구현 결과에서 제외하고 과거 증거로 보존
- README, 미션 요약과 요구사항 추적표에 순수 MAC 재측정 대기 상태 반영

### 정적 확인

- `calculate_mac()` 직접 호출 경로가 사용자 입력, JSON 판정과 성능 측정 세 곳임을 확인
- 사용자 입력과 JSON 판정에서 MAC 호출 전에 각각의 입력 검증이 완료됨을 코드로 대조
- 2차원·1차원 타이머 안에 파일 읽기, 입력 처리, 행렬 검증과 변환이 없음을 코드로 대조

### Codex 실행 여부

- 애플리케이션 실행 없음
- 유닛 테스트 작성 및 실행 없음
- 새 평균·표준편차와 성능 로그 생성 없음

### 다음 작업

사용자가 `python3 main.py`의 메뉴 2를 실행해 네 필수 크기의 순수 2차원·1차원 평균·표준편차와 JSON 전체 집계를 전달한다. 새 결과를 README와 증거에 반영한 뒤 메뉴 1의 순수 MAC 통계도 필요하면 별도로 확인한다.

## 2026-08-12 — MAC 워밍업과 측정 방식 비교 구현

### 변경 파일

- `src/mini_npu.py`
- `src/performance.py`
- `src/manual_mode.py`
- `src/report.py`
- `benchmark_compare.py`
- `MISSION.md`
- `README.md`
- `docs/requirements.md`
- `docs/progress.md`
- `docs/worklog.md`

### 수행 내용

- 개인 실험 뒤 `PERFORMANCE_REPETITIONS`에 남아 있던 1,000회를 공식 미션 설정 10회로 복구
- 2차원과 1차원 MAC 측정 전에 같은 입력으로 10회 워밍업하도록 구성
- 워밍업, 입력 검증과 1차원 변환은 타이머와 측정 통계에서 제외
- 메뉴 1과 메뉴 2 출력에 워밍업 횟수와 단일 MAC 독립 측정 조건 추가
- 공식 실행과 분리한 `benchmark_compare.py` 추가
- 개별 MAC 호출을 각각 측정하는 10·100·1,000회 비교 구현
- 타이머 한 쌍 안에서 MAC을 10·100·1,000회 실행하는 묶음 측정 구현
- 각 묶음 크기를 10번 측정하고 호출당 평균 10개의 평균·모집단 표준편차 계산
- 개별 표준편차와 묶음별 호출당 평균 표준편차의 의미를 출력에서 구분
- 네 필수 크기, 같은 패턴·Cross 필터와 같은 2차원·1차원 MAC 함수를 두 측정 방식에 재사용
- README에 공식 재현 조건, 워밍업의 통제 범위와 비공식 비교 목적을 기록
- 실행하지 않은 공식 성능값과 비교 결과는 증거 및 README 결과표에 작성하지 않음
- 후속 요청에 따라 비교 프로그램이 10·100·1,000 중 한 값을 명령행 인자로 받도록 변경
- 비교 설정을 한 번에 모두 실행하지 않고 설정별 로그를 따로 수집하도록 구성
- 공식·비공식 성능 표 출력에 변동계수 CV를 추가하고 같은 열 구성과 정렬을 공유
- 묶음 표의 반복 횟수는 타이머 안의 MAC 호출 수이며 묶음 표본 수 10개는 측정 방식 설명으로 구분

### 정적 확인

- 공식 성능 반복 설정이 10회인지 확인
- 2차원·1차원 워밍업이 각 타이머 시작 전에 실행되는지 대조
- 비교 프로그램의 검증, 1차원 변환과 출력이 타이머 밖에 있는지 대조
- 묶음 전체 시간을 묶음 크기로 나눈 호출당 평균을 표본으로 사용하는지 대조

### Codex 실행 여부

- 애플리케이션 실행 없음
- 비교 프로그램 실행 없음
- 유닛 테스트 작성 및 실행 없음
- 새 평균·표준편차와 PASS/FAIL 결과 생성 없음

### 다음 작업

사용자가 새 Python 프로세스에서 `python3 main.py`의 메뉴 2를 먼저 실행하고, 이어서 `python3 benchmark_compare.py 10`, `100`, `1000`을 각각 실행해 실제 출력을 전달한다.

## 2026-08-12 — 묶음 벤치마크 분리와 기존 비교 로그 보존

### 사용자 제공 실행 정보

- 실행 명령: `python3 benchmark_compare.py 10`, `100`, `1000`
- 실행 코드: 묶음 전용으로 분리하기 전의 개별·묶음 결합형 프로그램
- 공통 워밍업: 10회, 측정 제외
- 결과 범위: 각 설정의 개별 2차원·1차원과 묶음 2차원·1차원 평균·표준편차·CV
- 터미널 프롬프트의 계정명과 장치명은 증거에서 제외
- 실제 결과: `evidence/individual-batch-comparison.md`

### 확인 결과

- 10·100·1,000회 설정에서 네 필수 크기와 두 MAC 방식이 모두 출력됨
- 묶음 측정은 대체로 타이머 비용이 분산되며 작은 행렬에서 개별 측정보다 낮은 호출당 평균을 보임
- 반복 횟수가 늘어도 개별 호출 표준편차는 항상 감소하지 않았으며 1,000회 개별 2차원 결과에서 큰 CV가 관측됨
- 결합형 프로그램은 개별 측정을 먼저 실행한 뒤 묶음 측정을 수행해 캐시, CPython 적응형 실행과 CPU 상태의 순서 영향을 분리할 수 없음

### 변경 파일

- `benchmark_compare.py` 삭제
- `benchmark_batch.py` 추가
- `evidence/individual-batch-comparison.md`
- `MISSION.md`
- `README.md`
- `docs/requirements.md`
- `docs/progress.md`
- `docs/worklog.md`

### 수행 내용

- 비교 프로그램에서 개별 호출 측정을 제거하고 묶음 2차원·1차원 측정만 유지
- 파일명을 역할에 맞게 `benchmark_batch.py`로 변경
- 인자로 받은 10·100·1,000을 타이머 한 쌍 안의 MAC 호출 수로 사용
- 각 설정의 묶음 표본 수는 10개로 고정하고 측정 방식 설명에 표시
- 공식 일괄 분석과 같은 평균·표준편차·CV·N²·반복 횟수 표 출력 재사용
- `--10`은 기능 옵션처럼 보이므로 사용하지 않고 필수 숫자 값인 위치 인자 `10` 유지
- 기존 결합형 실행값은 순서 영향의 한계와 함께 과거 증거로 보존
- 최신 묶음 전용 결과는 실행하지 않았으므로 새 성능값을 작성하지 않음

### Codex 실행 여부

- 애플리케이션 실행 없음
- 묶음 전용 벤치마크 실행 없음
- 유닛 테스트 작성 및 실행 없음
- 사용자가 제공한 결합형 비교 출력만 증거에 반영

### 다음 작업

사용자가 `python3 benchmark_batch.py 10`, `100`, `1000`을 각각 새 프로세스에서 실행해 묶음 전용 결과를 전달한다.

## 2026-08-12 — 묶음 전용 벤치마크 결과 반영

### 사용자 제공 실행 정보

- 실행 명령: `python3 benchmark_batch.py 10`, `100`, `1000`
- 각 설정을 별도 Python 프로세스로 실행
- 워밍업 10회 제외 후 묶음 표본 10개 측정
- 네 필수 크기의 2차원·1차원 평균·표준편차·CV 출력 확인
- 실제 결과: `evidence/batch-benchmark.md`

### 분석 결과

- 세 설정의 호출당 평균 범위는 모든 항목에서 약 8% 이내
- 13×13·25×25 평균은 대부분 약 4% 이내 범위로 유지
- 설정별 8개 CV의 평균은 10회 약 `1.12%`, 100회 약 `1.02%`, 1,000회 약 `1.89%`
- 이번 실행에서는 100회가 가장 낮은 평균 CV를 보였지만 2차원 25×25 CV `4.09%` 예외 확인
- 1,000회는 1차원 5×5 `6.79%`, 13×13 `3.51%`로 100회보다 항상 안정적이지 않음
- 분리 전 10회 묶음 2차원 13×13 CV `20.21%`가 묶음 전용 실행에서 `0.79%`로 감소
- 프로그램 분리 방향과 일치하지만 시스템 상태 차이가 있어 감소 전체를 분리 효과로 단정하지 않음

### 변경 파일

- `evidence/batch-benchmark.md`
- `README.md`
- `docs/requirements.md`
- `docs/progress.md`
- `docs/worklog.md`

### Codex 실행 여부

- 애플리케이션 실행 없음
- 벤치마크 실행 없음
- 유닛 테스트 작성 및 실행 없음
- 사용자가 제공한 최신 묶음 전용 실제 출력만 기록하고 분석

### 다음 작업

사용자가 새 Python 프로세스에서 `python3 main.py`의 메뉴 2를 먼저 실행해 공식 워밍업 후 개별 10회 결과를 전달한다.

## 2026-08-12 — 기존 개별·묶음 비교 로그 분석

### 변경 파일

- `evidence/individual-batch-comparison.md`
- `README.md`
- `docs/progress.md`
- `docs/worklog.md`

### 분석 결과

- 분리 전 결합형 로그의 3개 설정과 2개 자료 구조, 4개 크기를 합친 24개 항목 비교
- 묶음 호출당 평균은 24개 중 20개에서 개별 호출 평균보다 낮음
- 묶음 CV는 24개 중 20개에서 개별 호출 CV보다 낮음
- 10회 묶음 2차원 13×13, 100회 묶음 1차원 13×13 등 묶음 CV가 더 큰 예외도 확인
- 타이머 한 쌍의 비용이 여러 MAC 호출에 분산되며 짧은 행렬의 호출당 평균이 낮아진 것으로 해석
- 개별 CV와 묶음별 호출당 평균 CV는 의미가 달라 실제 MAC 지연의 안정성 향상으로 단정하지 않음
- 기존 결합형 실행에는 개별 측정 뒤 묶음 측정이 이어지는 순서 영향이 있어 최신 묶음 전용 결과와 구분

### 문서 반영

- README에는 전체 결과표를 반복하지 않고 핵심 관찰, 기존 방식의 한계와 보완 방향만 요약
- 설정별 전체 수치와 예외는 증거 링크에서 확인하도록 구성
- 최신 `benchmark_batch.py`의 개선 여부는 새 사용자 실행 전이므로 검증 완료로 기록하지 않음

### Codex 실행 여부

- 애플리케이션 실행 없음
- 묶음 전용 벤치마크 실행 없음
- 유닛 테스트 작성 및 실행 없음
- 기존 사용자 제공 로그만 계산·분석해 반영

## 2026-08-12 — 동료평가 학습 가이드 작성

### 변경 파일

- `docs/peer-review-guide.md`
- `README.md`
- `docs/progress.md`
- `docs/worklog.md`

### 수행 내용

- 미션 배경부터 사용자 입력, JSON 검증, MAC 판정, 결과 집계와 성능 분석까지 발표 순서로 정리
- 미션 원문이 요구하는 MAC, 라벨 정규화, epsilon, O(N²)와 실패 원인 분류를 구현 파일에 연결
- 실제 JSON PASS 3건·FAIL 3건과 동점 정책에 따른 직접 원인을 소수 첫째 자리 점수로 정리
- 입력 검증·워밍업·I/O가 공식 타이머 밖에 있고 순수 `calculate_mac()` 호출만 측정되는 파이프라인 설명
- 평균, 모집단 표준편차, CV와 공식 개별 측정·비공식 묶음 측정의 차이 정리
- 1차원 MAC, 필터 생성기, 대규모 행렬과 CPU/NPU 설명 및 예상 질문·답변 추가
- 검증 포함 과거 성능 로그를 최신 공식 결과와 구분하고 재측정 기록란과 당일 점검표 추가
- README 관련 문서에 학습 가이드 링크 추가

### Codex 실행 여부

- 애플리케이션 실행 없음
- 벤치마크 실행 없음
- 유닛 테스트 작성 및 실행 없음
- 기존 코드, 미션 문서와 사용자 제공 실행 증거만 대조해 문서 작성

### 다음 작업

사용자가 새 Python 프로세스에서 `python3 main.py`의 메뉴 2를 먼저 실행해 공식 워밍업 후 2차원·1차원 개별 10회 결과를 전달한다.

## 2026-08-13 — 구현 단순화 검토안 작성

### 변경 파일

- `docs/simplification-review.md`
- `README.md`
- `docs/progress.md`
- `docs/worklog.md`

### 수행 내용

- 미션 필수·보너스 요구를 유지하면서 설명 부담을 줄일 다섯 가지 단순화 항목 정리
- 공식 성능 입력과 실행 중 저장소를 분리해야 하는 이유와 고정 입력 적용 방향 설명
- 사용자 N×N 모드의 JSON 탐색·자동 대체를 제거하고 메뉴 3 재사용 흐름으로 단순화하는 안 작성
- 공식 성능표에서 CV만 제거하고 평균·표준편차·N²·반복 횟수를 유지하는 안 작성
- 비공식 묶음 벤치마크 코드와 증거는 보존하고 README 설명만 축약하는 안 작성
- `_validated_number()` 중복 검증과 복잡한 타입 힌트를 정리할 범위 및 Python 3.8 호환 주의점 기록
- 유지할 현재 구현, 요구사항 누락 여부, 단계별 수정·검증 순서와 강의장 결정표 추가

### Codex 실행 여부

- 애플리케이션 실행 없음
- 벤치마크 실행 없음
- 유닛 테스트 작성 및 실행 없음
- 현재 코드와 미션 원문을 정적으로 대조해 검토 문서만 작성

### 다음 작업

사용자가 강의장에서 `docs/simplification-review.md`의 항목별 결정표를 검토한 뒤, 승인한 단순화 항목을 1번부터 한 단계씩 구현한다.

## 2026-08-13 — 데이터 흐름과 성능 입력 단순화 구현

### 승인된 방향

- 필수 3×3 직접 입력은 생성 데이터와 독립적으로 유지
- 모드 1의 N×N 방식은 저장 생성 필터를 사용하고 없으면 즉시 생성
- 모드 1에서 `data.json`을 탐색하지 않음
- `data.json`은 메뉴 2 진입 시 한 번 읽고 같은 모드 안에서 재사용
- 공식 네 크기는 고정 입력, 생성 데이터는 별도 보너스 성능 표 사용
- CV는 평균 대비 표준편차의 상대 크기를 보는 참고 지표로 유지
- README는 미션 권장 템플릿 중심으로 축약하고 묶음 실험은 증거 링크로 분리
- 미션 필수 검증은 유지하고 중복 점수 변환과 복잡한 타입 표현은 축소

### 코드 변경

- 실행 중 저장소를 생성 데이터 전용 `GeneratedStore`로 변경
- `GeneratedCase`에 Cross/X 필터와 최신 성능 패턴을 명시적으로 보관
- 직접 3×3 입력이 공용 저장소를 갱신하던 동작 제거
- 모드 1 N×N 방식의 JSON 로드·오류 대체 흐름 제거
- JSON 케이스가 생성 저장소에 들어가던 동작 제거
- 공식 성능 입력을 코드 기준 3×3과 JSON 기준 5·13·25 크기로 고정
- 생성 데이터의 2차원·1차원 성능 측정과 별도 표 출력 추가
- `classify_scores()`의 반복 타입 변환을 제거하고 epsilon과 MAC 결과 유한성 검사는 유지
- 데이터 클래스와 간단한 타입은 유지하고 긴 중첩 함수 타입 힌트 축소
- 묶음 벤치마크가 공식 고정 입력 선택 함수를 사용하도록 연결

### 문서 변경

- `README.md`를 실행 방법·구현 요약·결과 리포트·보너스 중심으로 축약
- `docs/simplification-review.md`를 검토안에서 확정 결정 문서로 갱신
- `MISSION.md`, 요구사항, 진행 상태와 동료평가 가이드의 데이터 흐름 동기화
- 과거 실행 증거는 당시 실제 결과로 보존하고 최신 실행값은 작성하지 않음

### 변경 파일

- `benchmark_batch.py`
- `src/data_loader.py`
- `src/json_mode.py`
- `src/manual_mode.py`
- `src/menu.py`
- `src/mini_npu.py`
- `src/pattern_generator.py`
- `src/performance.py`
- `src/report.py`
- `README.md`
- `MISSION.md`
- `docs/simplification-review.md`
- `docs/peer-review-guide.md`
- `docs/requirements.md`
- `docs/progress.md`
- `docs/worklog.md`
- `evidence/generated-filter-performance.md`
- `evidence/clean-performance-output.md`
- `evidence/manual-nxn-filter-flow.md`

### Codex 실행 여부

- 애플리케이션 실행 없음
- 벤치마크 실행 없음
- 유닛 테스트 작성 및 실행 없음
- 코드 호출 관계와 문서만 정적으로 대조

### 다음 작업

사용자가 직접 입력, N×N 자동 생성·재사용, 메뉴 3 교체와 메뉴 2의 공식·생성 성능 표를 수동 검증한다. 실제 출력을 받은 뒤 새 증거와 README 성능값을 반영한다.

## 2026-08-13 — 공식·생성 성능표 통합

### 사용자 확인

- 사용자가 직전 단순화 구조를 직접 실행해 문제가 없음을 확인
- 공식 성능표 아래에 생성 필터 표가 따로 나오는 출력은 크기별 비교가 불편하다고 판단
- 공식 입력 보호는 유지하면서 공식·생성 결과를 같은 표에 크기순으로 출력하도록 요청

### 코드 변경

- 성능 결과에 `공식`, `생성`, `묶음` 출처를 표시하는 필드 추가
- 공식 결과와 생성 결과를 측정 후 합쳐 크기 오름차순으로 정렬
- 같은 크기에서는 공식 행을 먼저, 생성 행을 다음에 출력
- 성능 표 제목을 `2차원 MAC 성능`, `1차원 MAC 성능`으로 통일
- 표 위의 `측정 방식` 문구 제거
- 별도로 출력하던 생성 필터 성능표 함수 제거
- 비공식 묶음 벤치마크도 공통 표 함수의 간결한 형식에 맞춤

### 문서 변경

- README, 미션 요약, 요구사항, 진행 상태, 단순화 결정과 동료평가 가이드를 통합 표 구조로 동기화
- 공식 입력과 생성 입력은 계속 분리되며 출력만 합쳐진다는 점을 명시

### Codex 실행 여부

- 애플리케이션 실행 없음
- 벤치마크 실행 없음
- 유닛 테스트 작성 및 실행 없음
- 코드 호출 관계와 출력 구성만 정적으로 점검

### 다음 작업

사용자가 생성 필터를 준비한 뒤 메뉴 2에서 2차원·1차원 표의 공식·생성 행이 크기순으로 함께 출력되는지 확인한다.

## 2026-08-13 — 성능표 기본 행 표기 축약

- 사용자 요청에 따라 `구분` 열의 `공식` 문구를 출력하지 않도록 변경
- 기본 성능 행은 공란으로 두고 생성 필터 행에만 `생성` 표시 유지
- 같은 크기에서 기본 행 다음에 생성 행이 나오는 정렬 순서는 유지
- 관련 설명 문서를 최신 출력 형식에 맞게 갱신
- 애플리케이션·벤치마크·유닛 테스트 실행 없음

## 2026-08-13 — 성능표 구분 열 제거

- 사용자 요청에 따라 성능 결과표의 `구분` 열을 완전히 제거
- `공식`, `생성`, `묶음` 출처 필드와 출력 로직 제거
- 공식·생성 결과를 합친 뒤 크기만 기준으로 정렬하는 흐름 유지
- 표 제목과 평균·표준편차·CV·N²·반복 횟수 열은 유지
- 관련 문서를 출처 구분 없는 통합 표 기준으로 갱신
- 애플리케이션·벤치마크·유닛 테스트 실행 없음

## 2026-08-13 — 성능표 정렬과 모드 1 출력 통합

### 사용자 요청

- 한글 헤더와 숫자 데이터가 같은 열에 맞지 않는 성능표 정렬 개선
- 모드 1의 문장형 성능 출력을 모드 2와 같은 표 형식으로 변경
- 모드 1 성능표 제목은 `MAC 성능`으로 단순화

### 코드 변경

- 표의 헤더와 실제 값을 기준으로 열 너비를 계산
- 한글 전각 문자의 터미널 표시 폭을 두 칸으로 반영하는 공백 처리 추가
- 헤더는 가운데, 크기는 왼쪽, 숫자는 오른쪽으로 정렬
- 열별 구분선을 추가해 헤더와 데이터 경계를 일치시킴
- 모드 1 측정값을 `PerformanceResult`로 구성해 공통 `print_performance_table()`로 출력
- 기존 모드 1의 평균·표준편차·워밍업·측정 대상 문장 출력 제거

### Codex 실행 여부

- 애플리케이션 실행 없음
- 벤치마크 실행 없음
- 유닛 테스트 작성 및 실행 없음
- 공통 표 호출 관계와 코드 형식만 정적으로 확인

### 다음 작업

사용자가 모드 1과 메뉴 2를 실행해 한글 헤더, 구분선과 숫자가 열 경계에 맞고 두 모드가 같은 표 형식을 사용하는지 확인한다.

## 2026-08-13 — 중복 없는 크기 기반 MAC 성능 분석

### 사용자 확인과 결정

- 생성한 크기가 필수 크기와 같을 때 성능표에 같은 크기가 두 번 나오는 문제 확인
- 미션 원문은 크기별 MAC 시간을 요구하지만 기존 필터·패턴 값을 성능 입력으로 강제하지 않음을 확인
- 같은 float 자료형과 같은 값의 기준 행렬을 사용해 크기와 2차원·1차원 계산 방식에 집중하기로 결정

### 코드 변경

- 필수 크기 `{3, 5, 13, 25}`와 생성 크기를 `set` 합집합으로 구성
- 같은 크기는 한 번만 측정하고 결과표에도 한 행만 출력
- 크기마다 `1.0`으로 채운 N×N float 행렬 두 개를 생성하는 공통 함수 추가
- JSON 패턴·필터와 생성 필터의 실제 값을 성능 입력에서 제거
- 모드 1도 실제 입력 내용 대신 선택한 크기의 기준 행렬로 성능 측정
- 1차원 방식은 같은 기준 행렬을 펼쳐 계산하므로 2차원과 입력 조건 통일
- 생성 저장소에서 성능용 최신 패턴을 제거하고 생성 필터만 보관
- 필터 하나만 감싸던 `GeneratedCase`를 제거하고 `{크기: Cross/X 필터}` 구조로 단순화
- 비공식 묶음 벤치마크도 같은 기준 행렬 생성 함수를 사용하도록 변경

### 문서 변경

- README, 미션 요약, 요구사항, 진행 상태, 단순화 결정과 동료평가 가이드를 크기 기반 측정으로 갱신
- 과거 실제 로그는 보존하고 당시 입력 방식과 최신 기준 행렬 방식의 차이를 상단에 표시

### Codex 실행 여부

- 애플리케이션 실행 없음
- 벤치마크 실행 없음
- 유닛 테스트 작성 및 실행 없음
- 함수 호출 관계, 제거한 이름의 잔여 참조와 코드 형식만 정적으로 점검

### 다음 작업

사용자가 필수 크기와 같은 크기 하나, 새로운 크기 하나를 생성한 뒤 메뉴 2를 실행해 중복 제거와 크기순 출력을 확인한다.

## 2026-08-13 — 기존 Cross/X 필터 기반 성능 측정

### 사용자 결정

- 성능용 `1.0` 행렬을 새로 만드는 대신 이미 존재하는 필터 두 개를 재사용
- MAC은 같은 크기 행렬 두 개면 되므로 패턴 없이 Cross/X 필터끼리 측정
- 같은 크기의 중복 결과 제거 정책은 유지

### 코드 변경

- 크기별 `1.0` 기준 행렬 생성 함수 제거
- 3×3은 코드 내부 기본 Cross/X 필터 쌍 사용
- 5×5·13×13·25×25와 기타 JSON 크기는 한 번 로드한 JSON 필터 쌍 사용
- JSON에 없는 생성 크기만 실행 중 저장 필터에서 추가
- JSON과 생성 저장소에 같은 크기가 있으면 JSON 필터를 우선해 한 번만 측정
- 모드 1은 방금 판정에 사용한 Cross/X 필터를 그대로 공통 성능 함수에 전달
- 1차원 방식은 같은 두 필터를 측정 전에 펼쳐 기존 MAC 함수로 측정
- 비공식 묶음 벤치마크도 기존 필터 쌍을 사용하도록 복원

### Codex 실행 여부

- 애플리케이션 실행 없음
- 벤치마크 실행 없음
- 유닛 테스트 작성 및 실행 없음
- 제거한 함수 참조, 필터 병합 우선순위와 공백 오류만 정적으로 점검

### 다음 작업

사용자가 JSON과 같은 크기 하나와 새로운 크기 하나를 생성한 뒤 메뉴 2에서 중복 제거와 추가 크기 출력을 확인한다.

## 2026-08-13 — 모드 1·2 성능 반복 횟수 통합

- 모드 1의 별도 `TIMING_REPETITIONS` 상수 제거
- 모드 1이 `performance.py`의 `PERFORMANCE_REPETITIONS`를 직접 사용하도록 변경
- 모드 2의 2차원·1차원 성능 측정과 같은 반복 횟수 설정 공유
- 워밍업은 목적이 다른 준비 실행이므로 `WARMUP_REPETITIONS`로 별도 유지
- 관련 README, 미션 요약, 요구사항, 진행 상태와 동료평가 가이드 갱신
- 애플리케이션·벤치마크·유닛 테스트 실행 없음

### 다음 작업

사용자가 `PERFORMANCE_REPETITIONS`를 10·100·1000으로 설정해 모드 2 개별 측정 로그를 수집하고, 최종 코드는 10으로 복구한다.

## 2026-08-13 — 모드 2 출력 순서 정리

- 사용자 요청에 따라 전체 판정 요약을 MAC 성능 분석 앞으로 이동
- 모드 2의 마지막 출력이 `[2차원 MAC 성능]`, `[1차원 MAC 성능]` 순서가 되도록 변경
- 성능 분석 오류 시 이미 출력한 전체 요약을 다시 출력하지 않도록 중복 호출 제거
- 모드 1은 판정 결과 뒤에 `[MAC 성능]`이 나오는 기존 순서 유지
- 관련 README, 미션 요약, 진행 상태와 동료평가 가이드 갱신
- 애플리케이션·벤치마크·유닛 테스트 실행 없음

## 2026-08-13 — 필수 성능 및 반복 횟수 실험 결과 반영

### 사용자 실행 결과

- 모드 1 직접 입력: Cross `2.000000`, X `5.000000`, 판정 X
- 모드 1 3×3 MAC 10회 평균 `0.001635 ms`, 표준편차 `0.000323 ms`, CV `19.76%`
- 모드 2 JSON 6건: 통과 3건, 실패 3건
- 모드 2의 3×3·5×5·13×13·25×25 2차원·1차원 개별 10회 결과 확보
- 모드 2 개별 측정 100·1,000회 결과 확보
- 묶음당 MAC 10·100·1,000회, 묶음 표본 10개의 2차원·1차원 결과 확보
- 모든 실험 후 `PERFORMANCE_REPETITIONS = 10` 복구 상태 확인

### 증거 정리

- `evidence/simplified-final-run.md`에 미션 제출용 모드 1·2와 개별 10회 결과 기록
- `evidence/performance-repetition-experiments.md`에 개별·묶음 10·100·1,000회 결과와 비교 분석 기록
- 기존 `evidence/batch-benchmark.md`는 과거 실행값으로 보존하고 최신 증거 링크 추가

### 결과 해석

- 개별 10회에서 네 크기 모두 1차원 평균 시간이 2차원보다 낮았음
- 개별 100회 평균 CV는 `2.97%`로 개별 10회 `4.95%`보다 낮았음
- 개별 1,000회 평균 CV는 `26.55%`였으며 일부 항목에서 큰 시스템 변동이 발생함
- 묶음 평균 CV는 10회 `0.90%`, 100회 `1.27%`, 1,000회 `1.30%`였음
- 반복 횟수가 증가해도 모든 항목의 CV가 항상 감소하지 않음을 확인
- 개별 호출과 묶음별 호출당 평균은 표본 단위가 달라 CV를 같은 의미로 직접 비교하지 않음

### 문서 반영

- README에는 미션에 필요한 모드 1과 모드 2 개별 10회 결과만 반영
- 100·1,000회 개별 결과와 모든 묶음 결과는 추가 실험 증거로 분리
- 요구사항과 진행 상태를 최신 실제 검증 결과에 맞게 갱신

### 미검증 항목

- JSON과 같은 5×5 생성 필터가 성능표에 중복되지 않는지
- JSON에 없는 7×7 생성 필터가 크기순 위치에 추가되는지

## 2026-08-13 — 7×7 생성 필터 성능 분석 재사용 결과 반영

### 사용자 실행 결과

- 메뉴 3에서 7×7 Cross/X 필터 생성
- Cross의 중앙 행·열과 X의 양쪽 대각선 모양 확인
- 같은 실행에서 메뉴 2를 선택해 생성 필터 재사용
- 2차원·1차원 성능표 모두 `3×3, 5×5, 7×7, 13×13, 25×25` 순서로 출력
- 7×7 2차원 평균 `0.003174 ms`, 1차원 평균 `0.001879 ms`, 반복 횟수 10 확인
- JSON 판정 결과는 전체 6건, 통과 3건, 실패 3건으로 유지

### 증거 정리

- 실제 생성 행렬과 성능 결과를 `evidence/generated-filter-merge.md`에 기록
- 새 생성 크기의 추가·모드 간 재사용·오름차순 출력을 검증 완료로 변경
- JSON과 같은 5×5 생성 시 중복 제거는 별도 실행 전이므로 미검증 유지
- README는 사용자 수정 방향 확인 전이므로 이번 작업에서 수정하지 않음

### Codex 실행 여부

- 애플리케이션 실행 없음
- 벤치마크 실행 없음
- 유닛 테스트 작성 및 실행 없음

### 다음 작업

같은 실행에서 5×5 필터를 생성한 뒤 메뉴 2의 두 성능표에 5×5가 한 번만 출력되는지 확인한다.
