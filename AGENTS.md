# AGENTS.md

## 목적

이 문서는 Codex가 E1-3 Mini NPU 시뮬레이터 미션을 작은 단계로 안전하게 수행하기 위한 작업 지침이다.

## 작업 시작 전에 반드시 확인할 문서

다음 문서를 순서대로 읽는다.

1. `MISSION.md`
2. `docs/requirements.md`
3. `docs/progress.md`

문서보다 현재 코드와 Git 상태를 우선한다. 문서와 실제 상태가 다르면 임의로 맞추지 말고 차이를 먼저 보고한다.

## 기본 작업 원칙

- 전체 미션을 한 번에 구현하지 않는다.
- 한 번에 하나의 논리적 단계만 처리한다.
- 각 단계는 구현 방향 설명, 사용자 승인, 구현 및 상세 설명 순서로 진행한다.
- 구현할 때 진행 상태, 작업 기록과 README 등 관련 문서를 함께 갱신한다.
- 필수 요구사항을 먼저 처리하고 보너스는 별도 승인 후 진행한다.
- 외부 라이브러리를 추가하지 않는다. Python 표준 라이브러리만 사용한다.
- MAC 연산은 반복문으로 직접 구현한다.
- Codex는 유닛 테스트 코드를 작성하거나 유닛 테스트를 실행하지 않는다.
- 테스트는 사용자가 직접 수행하며 Codex는 필요한 수동 검증 방법만 안내한다.
- 임시 Python 셸이나 일회성 Python 코드로 검증하는 예시는 안내하지 않는다.
- 실행 로그나 화면 캡처가 필요한 경우 구현을 마친 뒤 명령, 캡처 범위와 파일명을 안내한다.
- 실행 로그는 개인정보를 제거하고 목적, 명령, 입력, 실제 출력, 확인 결과와 미검증 항목을 구분한 Markdown 파일로 저장한다.
- 화면 캡처는 입력 재시도처럼 화면 흐름 자체가 증거인 경우에만 사용한다.
- 실행하지 않은 결과, 로그, 측정값, PASS/FAIL 결과와 스크린샷을 만들지 않는다.
- `data.json`의 실제 구조를 확인하기 전에는 세부 스키마를 추측해 고정하지 않는다.
- 사용자가 만든 변경사항을 임의로 삭제하거나 덮어쓰지 않는다.
- 관련 없는 파일을 수정하지 않는다.
- 민감정보, 개인 절대 경로, 계정명과 토큰을 저장하지 않는다.
- 기본 실행·검증 환경은 macOS와 zsh로 간주한다.
- Windows 검증은 별도 요구가 있을 때만 수행한다.
- commit, push, merge는 사용자가 직접 수행하며 Codex는 권장 커밋 메시지만 안내한다.
- 위험한 Git 명령과 삭제 명령을 임의로 실행하지 않는다.

## 구현 기준

- 행렬 데이터는 최소 3×3, 5×5, 13×13, 25×25를 처리해야 한다.
- 숫자는 정수와 실수를 모두 처리할 수 있도록 `float` 변환을 기본으로 검토한다.
- 사용자 입력 오류는 프로그램 종료가 아니라 해당 행의 재입력으로 처리한다.
- JSON의 케이스 오류는 가능한 한 케이스 단위 `FAIL`로 기록하고 다음 케이스를 계속 처리한다.
- 내부 표준 라벨은 `Cross`, `X`만 사용한다.
- 점수 비교는 epsilon 기반으로 수행한다.
- 성능 측정에서는 파일 읽기와 콘솔 출력을 제외한다.
- 작은 실행 시간을 측정할 때는 `time.perf_counter_ns()` 사용을 우선 검토한다.
- 실제 측정 반복 횟수와 측정 대상이 한 번의 MAC 호출인지 두 필터 판정 전체인지 출력과 README에 명시한다.

## `작업 시작` 입력 시

1. 저장소 루트인지 확인한다.
2. 현재 브랜치와 작업 트리를 확인한다.
3. 최근 커밋을 확인한다.
4. 미션·요구사항·진행 문서를 읽는다.
5. 실제 코드와 문서를 비교한다.
6. 완료·미검증·미구현 상태를 요약한다.
7. 다음 필수 작업 하나를 선정한다.
8. 수정할 파일, 검증 방법과 권장 커밋 메시지를 안내한다.
9. 위험하거나 모호한 부분이 없으면 해당 단계만 구현한다.

작업 우선순위는 다음과 같다.

1. 커밋되지 않은 기존 작업 마무리
2. 미해결 오류 해결
3. 현재 브랜치 작업 완료
4. 다음 미완료 필수 요구사항
5. 전체 실행 검증과 증거 수집
6. README 반영과 최종 검수
7. 보너스 과제

## `상태 확인` 입력 시

파일을 수정하지 않고 다음을 보고한다.

- 현재 브랜치와 변경사항
- 최근 커밋
- 완료·미검증·미구현 요구사항
- 확보·누락된 증거
- 미해결 문제
- 코드와 문서의 불일치
- 다음 권장 작업 하나

## `작업 종료` 입력 시

- 변경사항을 같은 조건으로 재검증한다.
- 실제 결과에 맞게 `docs/requirements.md`를 갱신한다.
- `docs/progress.md`에 현재 상태와 다음 작업을 기록한다.
- `docs/worklog.md`에 실행한 명령과 실제 결과를 기록한다.
- 실제 오류를 해결한 경우에만 `docs/troubleshooting.md`에 기록한다.
- 검증된 내용만 `README.md`에 반영한다.
- 필요한 스크린샷의 실행 명령, 캡처 범위와 파일명을 안내한다.
- Git 상태와 변경 파일을 요약한다.
- 권장 커밋 메시지 하나를 제안한다.

## 권장 검증 명령

저장소 루트에서 macOS zsh 기준으로 실행한다.

```zsh
python3 --version
python3 main.py
git status --short --branch
git diff --check
```

Codex는 위 검증 명령을 대신 실행하지 않으며, 사용자가 실제로 전달한 결과만 문서에 기록한다.
